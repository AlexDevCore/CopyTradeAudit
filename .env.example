# Copy this file to .env and fill in real values locally.
# Never commit the real .env file.
#
# SECURITY: paper trading needs NO wallet private keys and NO exchange
# credentials. Do not add any here. A private key would only ever be required
# by a future, separate, opt-in live module — never in this version.

# Anthropic (Claude) — OPTIONAL. Only used for explanations/reports, never in
# the trading loop. The app runs fully without it.
ANTHROPIC_API_KEY=
